WARNING: These scripts are not finished and don’t install Rigs of Rods at this stage.

Put the scripts in your user folder (Finder -> Go To -> user folder) and open a terminal (Finder -> Go To -> Utilities -> Terminal)

run these commands in this order and not simultaneously:

sh RoRdeps.sh 
(May ask you to install Xcode and CL-Tools which are required to proceed, will also ask for your sudo password (you should skip this and install without sudo) and to hit enter to install brew which you have to do)

sh RoRcore.sh

If you would like to update RoR re-run 
sh RoRcore.sh